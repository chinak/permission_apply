- 顶层 `pnpm-workspace.yaml` + 共享的 `package.json`/`vite.config.ts`/`postcss.config.mjs` 作为多包契约，`src/`（仓库应用）与 `permission_apply/`（权限申请应用）各自独立但复用同一套 Vite+Tailwind+shadcn/ui 构建管线。
- 通过根目录脚本 `fix_home.js`、`replace.js` 在根 `src/` 与 `permission_apply/src/` 之间双向复制 UI 组件与基础布局，保证两套 shadcn/ui 副本一致，避免跨包引用带来的版本漂移。
- 仓库应用 (`warehouse_app`) 作为外壳层，将权限申请页面以 iframe 或内嵌方式集成到统一路由与全局上下文（`AppContext`/`WarehouseContext`）中，形成「主应用 + 子应用」的聚合形态。
- 根级 `deploy.ps1` 调用 `deploy/deploy.sh` 并注入 `deploy/nginx-server.conf`/`nginx.conf` 模板，为所有子应用输出产物提供统一的备份、Nginx 反向代理与 SPA 路由回退策略。