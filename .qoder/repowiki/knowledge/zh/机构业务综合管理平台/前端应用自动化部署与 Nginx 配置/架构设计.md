- 核心入口为 `deploy.sh`，负责版本备份、文件同步、权限设置及 Nginx 配置生成与重载。
- 配置层包含 `nginx.conf`（生产环境模板，支持域名替换）与 `nginx-server.conf`（默认服务器块）。
- 产物目录 `dist/` 存放构建后的静态资源，通过脚本自动同步至目标路径 `/var/www/management-platform`。
- 依赖关系：脚本强依赖 Nginx 服务及 root 权限，通过 `sed` 动态注入域名配置。