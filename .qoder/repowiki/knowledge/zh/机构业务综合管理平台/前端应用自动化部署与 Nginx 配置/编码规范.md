- 静态资源（/assets/）采用长期缓存策略（expires 1y, immutable），而入口文件（index.html）强制不缓存以确保更新即时生效。
- Nginx 配置中统一启用 Gzip 压缩，针对 text/css、application/json 等常见 MIME 类型进行优化。
- 部署脚本遵循“先备份后替换”原则，在 Nginx 配置测试失败时自动恢复旧版本以保障服务可用性。