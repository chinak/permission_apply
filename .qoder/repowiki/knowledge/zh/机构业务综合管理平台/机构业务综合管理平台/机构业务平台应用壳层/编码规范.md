- 采用 React Context API 进行全局状态管理，并通过自定义 Hook（如 `useAppContext`）封装访问逻辑以增强类型安全与错误提示。
- 页面间状态传递主要依赖 `react-router` 的 `location.state`，用于在跳转时携带审批状态、拒绝原因等临时数据。
- UI 样式遵循 Tailwind CSS 约定，广泛使用 `cn` 工具函数处理条件类名拼接，保持组件样式的灵活性与一致性。