- 采用 pnpm workspace 进行多包管理，根目录与子应用共享统一的 `package.json` 依赖定义及 `vite.config.ts` 构建逻辑。
- 通过复制同步机制（如 `fix_home.js`, `replace.js`）在根目录与 `permission_apply` 间保持核心 UI 组件库（shadcn/ui）与基础布局的一致性。
- 根目录集成 PowerShell 部署脚本 (`deploy.ps1`) 与 Nginx 配置，为所有子应用提供标准化的生产环境发布与反向代理支持。