
  # Web Prototype Creation

  This is a code bundle for Web Prototype Creation. The original project is available at https://www.figma.com/design/pQrMCEc3IPs7z1OnOFhTIu/Web-Prototype-Creation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  